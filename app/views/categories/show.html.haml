= render "shared/header"
- breadcrumb :categories_show, @category
= render "layouts/breadcrumbs"
= render "shared/newItemBtn"
.default-container-gray
  .l-content
    %ul.parts-nav
      -# %li 今後ブランドに絞るリンクを作る予定のためコメントアウト
      -#   - @items_brand.each do |brand|
      -#     ="#{brand[:name]}"
          -# = link_to "#{brand[:name]}",brand_category3_brand_brand_group_path(@brand, id: "#{category[:id]}"), class: "parts-nav__list"
    %section.items-box-container
      %h1.items-box-container__head
        = "#{@category.name}の商品一覧"
      .items-box-container__content
        = render partial: "shared/item_list", collection: @items, as: :item
    .spacial-description
      .spacial-description__title
        %h3.font-white
          = @category.name
      .spacial-description__text
        %p
